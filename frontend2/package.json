{
    "name": "frontend2",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "wait-on http://localhost:3000/health && vite",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "typescript": "~5.9.3",
        "vite": "^7.1.7"
    },
    "dependencies": {
        "better-auth": "^1.3.31",
        "path": "^0.12.7",
        "wait-on": "^9.0.1"
    }
}
